import { PriceSearchPipe } from './price-search.pipe';

describe('PriceSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
